name: my_expensia
description: A private portfolio and expense tracker.
publish_to: none
version: 1.0.2+22

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  flutter_secure_storage: ^10.0.0
  flutter_local_notifications: ^22.3.0
  local_auth: ^3.0.1
  shared_preferences: ^2.5.5
  supabase_flutter: ^2.10.3
  workmanager: ^0.10.9
  timezone: ^0.11.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
